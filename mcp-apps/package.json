{
  "name": "@memory-server/mcp-apps",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:memory-browser && npm run build:memory-editor && npm run build:triage-dashboard && npm run build:tag-manager",
    "build:memory-browser": "APP=memory-browser vite build",
    "build:memory-editor": "APP=memory-editor vite build",
    "build:triage-dashboard": "APP=triage-dashboard vite build",
    "build:tag-manager": "APP=tag-manager vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "preact": "^10.29.1",
    "@modelcontextprotocol/ext-apps": "^1.7.1",
    "marked": "^11.2.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.5",
    "vite": "^5.4.21",
    "vite-plugin-singlefile": "^0.13.5",
    "tailwindcss": "^3.4.19",
    "postcss": "^8.5.14",
    "autoprefixer": "^10.5.0",
    "typescript": "^5.9.3"
  }
}
